Vagrant.configure(2) do |config|
	config.winrm.transport = :plaintext
	config.winrm.basic_auth_only = true
    config.winrm.username = "Fractalsol"
    config.winrm.password = "Fractalsol"
    config.vm.box = "win2016-primitive"
end